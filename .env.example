# ITSP-Automation ortam değişkenleri
# ÖNEMLİ: Bu dosyada PAROLA/KULLANICI ADI TUTULMAZ. Login SSO/MFA ile manuel yapılır.
# Sadece gizli olmayan (non-secret) yapılandırma değerleri buraya konabilir.

# ServiceNow instance temel adresi. config.yaml'daki base_url'i ezer (opsiyonel).
ITSP_BASE_URL=https://your-instance.service-now.com
